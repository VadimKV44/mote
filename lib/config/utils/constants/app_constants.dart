/// Константы приложения.
abstract final class AppConstants {
  // * ------------------------------- Blur ------------------------------- * //
  static const double kBlur = 10;

  // * ------------------------------ Opacity ----------------------------- * //
  static const double kOpacity0 = 0;
  static const double kOpacity05 = 0.5;
  static const double kOpacity1 = 1;

  // * ----------------------------- Durations ----------------------------- * //
  static const Duration kDuration200 = Duration(milliseconds: 200);
  static const Duration kDuration300 = Duration(milliseconds: 300);
  static const Duration kDuration500 = Duration(milliseconds: 500);
}
